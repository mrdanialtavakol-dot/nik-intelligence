# V0.11 — Executive UX Review

این نسخه بر اساس ساختار فعلی V0.10.1 بازطراحی شده و هدفش اضافه‌کردن Feature جدید نیست؛ هدف، تبدیل Prototype به تجربه‌ای آرام‌تر، سریع‌تر و قابل‌دفاع‌تر برای مدیرعامل است.

## ایرادهای اصلی نسخه قبل

1. Sidebar علاوه بر Navigation، دائماً کنترل‌های Demo را هم نشان می‌داد و تمرکز مدیر را کم می‌کرد.
2. Header در همه صفحات Brand-first بود؛ در محصول مدیریتی باید Context صفحه مهم‌تر از نام محصول باشد.
3. همه Cardها تقریباً Glass و دارای Shadow یکسان بودند؛ در نتیجه سلسله‌مراتب بصری ضعیف می‌شد.
4. API و n8n بیش از حد شبیه CTA اصلی دیده می‌شدند، در حالی که هنوز Placeholder هستند.
5. ML، Forecast و Anomaly در هر بار اجرای صفحه محاسبه می‌شدند؛ حتی وقتی صفحه به آن‌ها نیاز نداشت.
6. یک خطای Runtime در یک Workspace می‌توانست تجربه کل App را خراب کند.
7. چند نسل CSS روی هم قرار گرفته‌اند؛ V0.11 یک Final Design Layer قوی‌تر روی همه آن‌ها اعمال می‌کند تا خروجی یکپارچه بماند.

## اصلاحات V0.11

- Navigation-first Sidebar و مخفی بودن کنترل‌های Demo به‌صورت پیش‌فرض.
- Header کوچک‌تر و Page-first با Context واضح.
- سه سطح Surface: Primary / Metric / Utility به‌جای Glass یکسان برای همه چیز.
- API/n8n به Action ثانویه تبدیل شدند.
- Lazy execution برای ML/Forecast/Anomaly.
- Error Boundary در سطح صفحه.
- Typography و Contrast منظم‌تر.
- Motion ظریف و احترام به prefers-reduced-motion.
- Footer واحد و کوتاه.

## نتیجه مورد انتظار

مدیرعامل باید سریع‌تر بفهمد کجاست، چه چیزی مهم است و چه تصمیمی باید بگیرد؛ و سیستم در صفحات غیرتحلیلی سبک‌تر اجرا شود.
